🌐 Языки: [English](RELEASE_NOTESv1.5.0.md) | [Русский(текущий)](RELEASE_NOTESv1.5.0.ru.md)

# Примечания к выпуску - v1.5.0

## S.T.A.L.K.E.R. 2 Менеджер модов - Мультверсии модов и предупреждения по файлам

### Основные изменения

#### Поддержка нескольких версий одного мода
- **Группировка модов по базовому имени** – Моды с одинаковым базовым именем (разные папки/версии) теперь отображаются одной строкой в основном списке.
- **Отображение основной версии** – В списке показывается одна «основная» запись, а видимое имя зависит от включённых версий:
  - **0 включённых версий**: отображается только базовое имя мода.
  - **1 включённая версия**: базовое имя + выбранная версия в скобках, например `Quests (Quests - v1.0.1)`.
  - **>1 включённых версий**: базовое имя + пометка, что выбрано несколько версий.
- **Индикатор нескольких версий** – Добавлен отдельный значок, показывающий, что у мода есть несколько версий и/или включено несколько версий.

#### Улучшения управления файлами по версиям
- **Список версий в окне файлов мода** – В окне файлов мода теперь отображаются все версии выбранного мода (когда несколько папок имеют одно базовое имя).
- **Включение/выключение по версии** – У каждой версии есть свой чекбокс:
  - Включение версии включает все её файлы.
  - Отключение версии отключает все её файлы.
- **Управление файлами внутри версии** – Чекбоксы файлов продолжают работать по каждой версии отдельно; если все файлы версии отключены, версия автоматически выключается.

#### Улучшенные предупреждения и обработка пустых модов
- **Глобальное предупреждение по отключённым файлам** – Иконка предупреждения (⚠) в основном списке теперь учитывает **все включённые версии** мода:
  - Показывается только тогда, когда в хотя бы одной включённой версии есть отключённые файлы.
  - Текст подсказки локализован через ключ `SomeFilesDisabled`.
- **Пустые папки модов** – Моды, у которых в папке **нет ни одного файла**, теперь:
  - По умолчанию **не включаются** при загрузке из папки Vortex.
  - Показывают иконку ⚠ с локализованной подсказкой `NoFilesFound` («В папке мода нет файлов»).
  - Не могут быть включены для установки из основного списка (чекбокс не включится).

#### Безопасность установки
- **Блокировка установки некорректных модов** – Мод нельзя включить для установки, если:
  - В его папке нет файлов, или
  - Все его файлы (для всех версий) отключены.
- **Согласованность состояний между окнами** – Изменения в окне файлов мода (чекбоксы версий и файлов) корректно отражаются в основном списке: состояние включения и предупреждения обновляются.

### Технические улучшения

- **Расширение модели `ModInfo`**
  - Добавлены свойства: `DisplayName`, `IsPrimaryVersion`, `HasMultipleVersions`, `MultipleVersionsTooltip`, `InstalledVersionsCount`, `HasAnyFiles`, `HasAnyEnabledFiles`, `GroupIsEnabled`, `DisabledFilesTooltip`, `AggregatedHasDisabledFiles`.
  - Сеттер `IsEnabled` теперь защищает от включения модов без файлов или без единого включённого файла.
- **Улучшения в `ModManagerService`**
  - При загрузке модов из Vortex сервис рекурсивно сканирует папки, чтобы определить, есть ли в них файлы.
  - Пустые моды загружаются, но помечаются как выключенные и с `HasAnyFiles = false`.
- **Обновления UI**
  - Основной список модов теперь использует биндинги `DisplayName`, `GroupIsEnabled`, `HasMultipleVersions`, `HasMultipleInstalledVersions`, `DisabledFilesTooltip`, `AggregatedHasDisabledFiles`.
  - Добавлен `BooleanToBrushConverter` для наглядной подсветки индикатора нескольких установленных версий.
  - Окно файлов мода обновлено: добавлен список версий, выровнаны стили, улучшена локализация заголовка и кнопок.
- **Обновление локализации**
  - Добавлены/расширены ключи: `MultipleVersionsAvailable`, `SomeFilesDisabled`, `NoFilesFound` для EN/RU/FR.

### Как пользоваться

- **Для управления несколькими версиями одного мода**:
  - Откройте окно файлов мода и используйте список версий, чтобы переключаться между разными папками/версиями.
  - Используйте чекбокс версии, чтобы быстро включить/выключить все файлы этой версии.
- **Как читать предупреждения в основном списке**:
  - ⚠ + «Некоторые файлы отключены» – хотя бы один файл отключён в одной из включённых версий мода.
  - ⚠ + «В папке мода нет файлов» – в папке(ах) мода вообще нет файлов.
- **Чтобы мод гарантированно установился**:
  - Должна быть включена хотя бы одна версия мода **и** хотя бы один файл этой версии должен быть включён.

См. [README.md](README.md) для подробных инструкций по использованию.



<Window x:Class="Stalker2ModManager.Views.DlcModLoaderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Herbata's Mod as DLC Loader" Height="250" Width="500"
        Background="#FF1E1E1E" WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <!-- Hover color to match main window -->
        <SolidColorBrush x:Key="HoverBackgroundBrush" Color="#FF5A5A5A"/>
        
        <Style TargetType="Button" x:Key="BorderlessButtonStyle">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Match main Button style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000" BlurRadius="3" ShadowDepth="1"/>
                            </Border.Effect>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF007ACC"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#FF1E1E1E"/>
                    <Setter Property="Foreground" Value="#FF666666"/>
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Border BorderBrush="#FF007ACC" BorderThickness="2" CornerRadius="8">
        <Grid Margin="15,15,15,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <Grid Grid.Row="0" Margin="0,0,0,15" MouseDown="TitleBar_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Herbata's Mod as DLC Loader" 
                          FontSize="18" 
                          FontWeight="Bold"
                          Foreground="#FFFFFFFF"
                          VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="✕" Width="30" Height="30" Click="Close_Click"
                        Padding="0"/>
            </Grid>

            <!-- Content -->
            <StackPanel Grid.Row="1">
                <TextBlock Text="Load mods as DLCs" 
                          FontSize="14" 
                          Foreground="#FFD4D4D4"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20"/>
                
                <Button Content="Run" 
                        Click="RunButton_Click" 
                        HorizontalAlignment="Center"
                        Width="155" 
                        Height="35"
                        FontSize="14"
                        Margin="0,0,0,15"/>
                
                <TextBlock FontSize="11" 
                          Foreground="#FF888888"
                          HorizontalAlignment="Center"
                          Margin="0,10,0,5">
                    <Run Text="Author: " />
                    <Hyperlink NavigateUri="https://github.com/herbatka" RequestNavigate="AuthorLink_RequestNavigate">
                        <Run Text="https://github.com/herbatka" Foreground="#FF007ACC"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

            <!-- Links -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Style="{StaticResource BorderlessButtonStyle}" 
                        Width="32" Height="32" 
                        Click="GitHubImage_Click"
                        ToolTip="GitHub Repository" Cursor="Hand">
                    <Image Source="https://github.githubassets.com/assets/GitHub-Mark-ea2971cee799.png" Width="32"/>
                </Button>
                <Button Style="{StaticResource BorderlessButtonStyle}" 
                        Width="32" Height="32" 
                        Margin="5,0,0,0" 
                        Click="NexusModsImage_Click"
                        ToolTip="Nexus Mods" Cursor="Hand">
                    <Image Source="https://images.nexusmods.com/favicons/ReskinOrange/favicon-230x230.png" Width="32"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>

